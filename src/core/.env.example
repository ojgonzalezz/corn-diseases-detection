# Archivo de configuración de ejemplo para corn-diseases-detection
# Copia este archivo como .env y modifica los valores según tus necesidades

# === CONFIGURACIÓN DE IMAGEN ===
# Tamaño de las imágenes de entrada (ancho, alto)
IMAGE_SIZE=(224, 224)

# Número de clases a clasificar
NUM_CLASSES=4

# Nombres de las clases (deben coincidir con los nombres de carpetas en data/)
CLASS_NAMES=['Blight', 'Common_Rust', 'Gray_Leaf_Spot', 'Healthy']

# === CONFIGURACIÓN DE SPLIT DE DATOS ===
# Ratios para división train/val/test (deben sumar 1.0)
SPLIT_RATIOS=(0.7, 0.15, 0.15)

# === CONFIGURACIÓN DE BALANCEO ===
# Máximo número de imágenes a agregar para balancear clases
MAX_ADDED_BALANCE=50

# === CONFIGURACIÓN DE ENTRENAMIENTO ===
# Tamaño del batch de entrenamiento
BATCH_SIZE=32

# Número máximo de épocas
MAX_EPOCHS=30

# Learning rate inicial
LEARNING_RATE=0.001

# Tasa de dropout
DROPOUT_RATE=0.3

# === CONFIGURACIÓN DE TUNER (Keras Tuner) ===
# Número máximo de trials para búsqueda de hiperparámetros
MAX_TRIALS=10

# Épocas por trial
TUNER_EPOCHS=10

# Factor de reducción de LR en callbacks
FACTOR=3

# === CONFIGURACIÓN DE DATASETS ===
# Datasets a considerar para análisis
DATASETS_CONSIDERATION=['no-augmentation', 'augmented']

# === CONFIGURACIÓN DE SIMILARIDAD ===
# Umbral de similitud para filtrado de imágenes duplicadas
IM_SIM_THRESHOLD=0.95

# === CONFIGURACIÓN DE PROYECTO ===
# Versión del proyecto
PROJECT_VERSION=0.1.0

# Nombre del proyecto
PROJECT_NAME=corn-diseases-detection

# === CONFIGURACIÓN DE MODELOS ===
# Backbone por defecto
BACKBONE=MobileNetV3Large

# === CONFIGURACIÓN DE EXPERIMENTOS ===
# Experimento por defecto para MLflow
EXPERIMENT_NAME=edge_models_comparison
